// import { Inter } from '@next/font/google'

// const inter = Inter({ subsets: ['latin'] })

// Plinko game 
// Plinko is a game where you drop a ball down a board and it bounces around until it falls into a slot.
// The goal is to get the ball into the slot with the highest value.

export default function Home() {
  return (
    <main>
    </main>
  )
}
